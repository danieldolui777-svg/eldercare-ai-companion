services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: eldercare
      POSTGRES_PASSWORD: eldercare
      POSTGRES_DB: eldercare_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U eldercare"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
